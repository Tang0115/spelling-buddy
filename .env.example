# Copy to .env.local and paste your key from https://dashboard.clerk.com/~/api-keys
# Must use the VITE_ prefix (not NEXT_PUBLIC_) — see https://vitejs.dev/guide/env-and-mode.html
VITE_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# Optional Puter.js cloud TTS / STT backend (off by default — use browser speech on Vercel unless you set this).
# VITE_USE_PUTER_AI=true
# With Puter on, use Whisper for spelling (optional): VITE_STT_PUTER=true
